let departure = "Berlin";
let arrival = "";
let driver = "";

if((departure != "" || arrival != "") && driver != "") {
  console.log('Train will start');
}
else {
  console.log(`Train can't start`);
}
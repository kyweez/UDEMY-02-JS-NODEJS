// Function declaration
function addition(number1, number2) {
    // Give the instruction with given arguments
    console.log(number1 + number2);
}

// Call the function with personalized arguments.
addition(5, 10);

